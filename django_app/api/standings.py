"""S2 — Standings/Leaderboard API. F1 placeholder.

F1 pre-stubs an EMPTY ``ninja.Router``. The S2 slice agent (see
``.plan/django-ninja-tasks.md``) fills the body of THIS module ONLY and must
never edit ``api/__init__.py`` or ``config/urls.py`` — both are FROZEN by F1.

Ports ``backend/app/api/standings.py`` (prefix ``/api/seasons``, main.py:108).
"""

from ninja import Router

router = Router()
