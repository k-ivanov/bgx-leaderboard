# Refactor: FastHTML → FastAPI + Vue 3 (Mosaic Lite) on Railway

> After approval, this file will be copied to `.plan/refactor-plan.md` at the repo root.

## Context

The app today is **FastHTML** (python-fasthtml), not FastAPI. Pages are generated by Python DSL (`Div`, `Table`, `Tr`, …) in `src/ui/`, with CSS embedded inline in `src/ui/styles.py`. There is no JavaScript, no templates dir, no build step, and no JSON API. Data is Postgres via SQLAlchemy 2 + Alembic. Deployment is a single Docker container on Railway.

The refactor splits the stack cleanly:

- **Backend** becomes a pure FastAPI JSON API under `/api/*`.
- **Frontend** becomes a Vue 3 SPA built with Vite, styled with **Mosaic Lite** (Tailwind) and themed to preserve the current dark racing aesthetic (gold/silver/bronze podium accents).
- **Deployment** stays a single Railway service: the Dockerfile builds the Vue bundle, copies `dist/` into the Python image, and FastAPI serves it as static files under `/app/*` with an SPA history-mode fallback.

Why: the current FastHTML layer couples rendering to Python and makes rich interactivity (filters, live search, charts, mobile polish) expensive. Moving to JSON + Vue unlocks a modern UI and keeps the existing database, migrations, services, and importers intact.

---

## Decisions (from this session)

| Decision | Choice | Notes |
|---|---|---|
| Deployment topology | **Single Railway service.** FastAPI serves Vue `dist/` with SPA fallback. | No CORS. One Dockerfile, one deploy, one domain. |
| Visual direction | **Mosaic Lite components, dark theme preserved.** | Tailwind `dark` class strategy + custom palette; keep gold/silver/bronze podium semantics. |
| Repo layout | **Monorepo** with `/backend` and `/frontend`. | Single git history, atomic cross-stack PRs. |
| URL scheme | **`/app/*` for the SPA, `/api/*` for the API.** | Existing `/2026/expert`-style URLs will **not** be preserved by default — see "Backward compatibility" consideration below. |

---

## Target architecture

```
bgx-navigation-dashboard/
├── backend/                           # moved from current src/
│   ├── app/
│   │   ├── main.py                    # FastAPI app factory, CORS off (same origin), static mount
│   │   ├── api/
│   │   │   ├── seasons.py             # /api/seasons, /api/seasons/{year}
│   │   │   ├── standings.py           # /api/seasons/{year}/standings, .../{category}
│   │   │   ├── events.py              # /api/seasons/{year}/events[/{slug}]
│   │   │   ├── results.py             # /api/seasons/{year}/categories/{cat}/events/{slug}
│   │   │   ├── riders.py              # /api/seasons/{year}/riders/{race_number}[/{slug}]
│   │   │   └── stats.py               # /api/stats (private analytics)
│   │   ├── schemas/                   # Pydantic response models (one file per resource)
│   │   ├── services/                  # unchanged — standings.py, rider.py move as-is
│   │   ├── db/                        # unchanged — models.py, session.py
│   │   ├── middleware/
│   │   │   └── visit_tracker.py       # replaces src/database.py logic as FastAPI middleware
│   │   ├── config.py                  # unchanged semantics
│   │   └── static.py                  # mounts /frontend/dist at /app, SPA fallback
│   ├── alembic/                       # unchanged
│   ├── alembic.ini
│   ├── scripts/                       # unchanged importers (import_2025, import_event, seed_calendar)
│   ├── tests/
│   │   ├── test_standings_2025.py     # keep golden test; adjust imports
│   │   └── test_api.py                # new: endpoint-level contract tests
│   ├── pyproject.toml                 # replace requirements.txt — pin FastAPI, drop python-fasthtml
│   └── Dockerfile -> ../Dockerfile    # single root Dockerfile multi-stage
├── frontend/
│   ├── index.html
│   ├── vite.config.ts                 # base: '/app/', proxy /api → backend in dev
│   ├── tailwind.config.js             # Mosaic Lite config + dark mode 'class' + BGX palette
│   ├── package.json
│   ├── tsconfig.json
│   ├── src/
│   │   ├── main.ts                    # createApp, router, pinia
│   │   ├── App.vue                    # shell: Mosaic Lite sidebar/topbar themed dark
│   │   ├── router/
│   │   │   └── index.ts               # history mode, base '/app/'
│   │   ├── stores/
│   │   │   └── season.ts              # pinia: current year, selected category
│   │   ├── api/
│   │   │   ├── client.ts              # fetch wrapper with base '/api'
│   │   │   └── types.ts               # TS types matching Pydantic schemas
│   │   ├── views/
│   │   │   ├── StandingsView.vue      # /app/:year/:category?
│   │   │   ├── EventsView.vue         # /app/:year/events
│   │   │   ├── EventResultsView.vue   # /app/:year/:category/:eventSlug
│   │   │   ├── EventOverviewView.vue  # /app/:year/events/:eventSlug
│   │   │   ├── RiderView.vue          # /app/:year/r/:raceNumber/:slug?
│   │   │   └── StatsView.vue          # /app/stats
│   │   ├── components/
│   │   │   ├── layout/{Sidebar,Topbar,YearSwitcher,CategoryTabs}.vue
│   │   │   ├── standings/StandingsTable.vue
│   │   │   ├── events/{EventCalendar,EventCard}.vue
│   │   │   ├── rider/{RiderHeader,RiderResultsTable,RiderDisambiguation}.vue
│   │   │   ├── stats/{StatCard,StatGrid,VisitTable}.vue
│   │   │   └── common/{PositionBadge,PointsPill,TimeCell,EmptyState,LoadingSkeleton,ErrorState}.vue
│   │   └── styles/
│   │       ├── tailwind.css           # @tailwind base/components/utilities
│   │       └── theme.css              # podium colors, typography (Inter + JetBrains Mono)
│   └── public/
├── Dockerfile                          # multi-stage: node build → python runtime
├── docker-compose.yml                  # updated: single service + postgres
├── railway.json                        # unchanged (builder: DOCKERFILE)
└── .plan/refactor-plan.md              # this plan (copied after approval)
```

### Request flow

```
Browser ──▶ Railway ──▶ FastAPI (Uvicorn)
                         ├── /api/*       → JSON (routers)
                         ├── /health      → JSON
                         ├── /app/*       → serve frontend/dist/* (StaticFiles)
                         │                  fallback → dist/index.html (SPA)
                         └── /            → 302 /app/
```

---

## Domain rule — rider identity

A rider is uniquely identified within a season by the tuple **`(race_number, first_name, last_name)`** — not by `race_number` alone. Race numbers can be reused across different people (common when riders share a number across categories, or when a number is reissued). This is already reflected in the current code:

- `services/rider.py::get_rider_profile(session, season, race_number, first, last)` takes all three fields.
- `services/rider.py::riders_sharing_number()` returns a disambiguation list.
- The current URL `/r/{race_number}/{slug}` uses a name-derived slug as the second key.

Implications for this refactor:

1. **API path keys.** `/api/seasons/{year}/riders/{race_number}` returns a list (disambiguation); `/api/seasons/{year}/riders/{race_number}/{slug}` is the singular resource. The slug MUST be deterministic from `first_name + last_name` (e.g., `slugify(f"{first} {last}")`) and stable across events/categories so deep links don't rot mid-season.
2. **Pydantic schemas.** The `Rider` response model exposes `{ race_number, first_name, last_name, slug, team?, bike? }`. Any rider identifier in nested payloads (e.g., a row in `StandingsRow`) must include `slug` alongside `race_number`, never just `race_number`.
3. **Frontend routing.** Links to a rider always build `/app/{year}/r/{race_number}/{slug}`. If code ever has only the number, it routes to the disambiguation view first.
4. **DB schema (verify, don't change yet).** Confirm the existing `Rider` table stores rows keyed compatibly with this rule. Current model note from exploration: `Rider [season + category unique]` on `race_number`, which means the same person in two categories is currently two `Rider` rows. That's fine for this refactor — the service layer already joins across categories in `get_rider_profile`. A future cleanup could introduce a `Person` entity (one row per `(season, race_number, first, last)`) with category memberships, but that is **out of scope here**.
5. **Tests.** Add an API contract test that exercises the disambiguation case: seed two riders sharing `(year, race_number)` but differing in names; hit `/api/seasons/{year}/riders/{race_number}` and assert the list; hit both slug URLs and assert the right profiles.

## Considerations (comprehensive)

### 1. Framework migration (FastHTML → FastAPI)

- `python-fasthtml` is removed entirely. All 11 current routes in `backend/src/routes.py` must be split into (a) JSON endpoints under `/api/*` and (b) Vue views that call them.
- Response shapes are designed from the service layer outward. `services/standings.py` and `services/rider.py` already return Python objects (`StandingsRow`, rider profile dicts) — wrap them in Pydantic schemas, don't rewrite.
- Visit tracking (`src/database.py`) moves to a FastAPI middleware that only records when `Accept: text/html` AND path starts with `/app/` (not API calls) to preserve current analytics semantics.

### 2. API design

One endpoint per resource, URL-addressable and cacheable. Proposed contract:

| Method | Path | Purpose |
|---|---|---|
| GET | `/api/seasons` | List all seasons with `is_current` flag |
| GET | `/api/seasons/{year}` | Season meta + categories + events |
| GET | `/api/seasons/{year}/standings` | All categories (for tab preview) or redirect |
| GET | `/api/seasons/{year}/standings/{category}` | Full standings table for one category |
| GET | `/api/seasons/{year}/events` | Event calendar |
| GET | `/api/seasons/{year}/events/{slug}` | Event meta (for overview page) |
| GET | `/api/seasons/{year}/categories/{cat}/events/{slug}` | Event results |
| GET | `/api/seasons/{year}/riders/{race_number}` | Rider list if multiple share number |
| GET | `/api/seasons/{year}/riders/{race_number}/{slug}` | One rider across categories |
| GET | `/api/stats` | Visit analytics |
| GET | `/health` | Health check (unchanged) |

All endpoints are `GET` and cacheable. Return Pydantic v2 models with `from_attributes=True` for ORM compatibility.

### 3. Backward compatibility (⚠️ decision tradeoff)

Choosing `/app/*` for frontend URLs **breaks existing links** like `/2026/expert`. To mitigate without giving up the clean split:

- Add redirect routes at the backend root: `GET /{year}` → `302 /app/{year}`, `GET /{year}/{category}` → `302 /app/{year}/{category}`, etc. Six redirect patterns cover every current URL shape.
- Keep `/` → `/app/` (was `/` → `/{current_year}/`).
- This is cheap to add and prevents link rot for any externally shared URLs.

### 4. Mosaic Lite + dark theme

Mosaic Lite is a **Tailwind 3+** admin template (Cruip, free tier available). Integration plan:

- Install Tailwind, configure `darkMode: 'class'`, set `<html class="dark">` by default.
- Copy the Mosaic Lite components we actually use (Sidebar, Topbar, Card, Table, Stat widgets) into `frontend/src/components/mosaic/` — don't vendor the whole template; cherry-pick.
- Override the palette in `tailwind.config.js`:
  - `primary`: gold/amber (current `#f59e0b`/`#fbbf24`)
  - `slate`/`gray` retuned toward the current `#0b0d10`/`#14171c` elevated-surface system
  - Add `podium-gold`, `podium-silver`, `podium-bronze` for position badges
- **Licensing**: verify Mosaic Lite free-tier license permits component extraction. Mosaic Lite (free) is MIT-licensed in its Cruip repo; Mosaic (pro) is not. Use only components from the free repo.

### 5. Build and deploy (single Railway service)

Multi-stage root `Dockerfile`:

```dockerfile
# Stage 1: build frontend
FROM node:20-alpine AS frontend-build
WORKDIR /app
COPY frontend/package*.json ./
RUN npm ci
COPY frontend/ ./
RUN npm run build  # outputs /app/dist

# Stage 2: python runtime
FROM python:3.12-slim
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends curl && rm -rf /var/lib/apt/lists/*
COPY backend/pyproject.toml backend/uv.lock* ./
RUN pip install --no-cache-dir uv && uv pip install --system -r pyproject.toml
COPY backend/ ./backend/
COPY --from=frontend-build /app/dist ./frontend_dist
ENV FRONTEND_DIST=/app/frontend_dist
EXPOSE 5001
CMD ["sh", "-c", "alembic -c backend/alembic.ini upgrade head && uvicorn backend.app.main:app --host 0.0.0.0 --port ${PORT:-5001}"]
```

`docker-compose.yml` stays a two-service dev setup (postgres + dashboard) but uses the new Dockerfile. `railway.json` is unchanged (builder: DOCKERFILE).

### 6. Dev ergonomics

- Frontend dev: `cd frontend && npm run dev` — Vite on `:5173`, proxies `/api/*` to backend on `:5001`.
- Backend dev: `cd backend && uvicorn app.main:app --reload --port 5001`.
- Root `Makefile` or `justfile` with `make dev` that runs both in parallel (via `concurrently` or `tmux`).
- Keep `start.sh` for the "just run the whole thing locally" case; update it to run the built frontend.

### 7. Testing

- **Backend**: keep `tests/test_standings_2025.py` (golden test over CSV import — critical). Add `tests/test_api.py` with FastAPI `TestClient`: assert schema, status codes, one happy-path per endpoint. Target: every API endpoint has one contract test.
- **Frontend**: Vitest for component unit tests (PositionBadge, PointsPill, TimeCell — pure rendering). Playwright (or Cypress) for one smoke e2e that boots the real backend against a seeded test DB.
- CI: GitHub Actions workflow runs `pytest`, `vitest run`, `npm run build` (catches TS/build errors).

### 8. Performance & caching

- API responses are all read-only and keyed by URL — set `Cache-Control: public, max-age=60, s-maxage=300` on `/api/seasons/*` endpoints. Railway sits behind Cloudflare-ish edge by default.
- Vite bundle: set `build.rollupOptions.output.manualChunks` to split vendor (vue, vue-router, pinia) from app code for better long-term caching.
- Static assets from `dist/` served with long cache + hashed filenames (Vite default).

### 9. SEO and social sharing

Current site is crawlable (pure HTML). A Vue SPA is not crawlable by default.

- **Minimum**: set `<title>`, `<meta name="description">`, OG tags dynamically via `@vueuse/head` per route.
- **If SEO matters**: consider SSR/SSG via Nuxt, or pre-render the 7-20 most-linked pages at build time with `vite-plugin-prerender`. Flag this as a decision needed before launch, not a blocker.

### 10. Accessibility

The current CSS has decent contrast and large tap targets. Preserve:
- Keyboard navigation for category tabs and year switcher (ARIA `role="tab"`, `aria-selected`).
- Focus rings (Tailwind `focus-visible:` with accent color).
- `prefers-reduced-motion` respected for any transitions.
- Position badges must not rely on color alone — keep the numeric text (already does).

### 11. Internationalization

Seed CSV headers are Bulgarian (`Поз`, `Ст.№`, `Състезател`). The UI is currently English. The refactor is a good moment to decide:
- Leave as English-only (simplest, matches current).
- Add vue-i18n with `en` + `bg` dictionaries (small cost now, saves pain later).

Recommend **English-only unless bilingual is a stated product goal** — defer i18n to a separate scope.

### 12. Visit tracking privacy

Current tracking records page + category + device type + timestamp, no PII. Keep it, but:
- Move recording to a background task (`BackgroundTasks`) so it never blocks response.
- Exclude `/api/*` from tracking; track only `/app/*` visits where `Accept` includes `text/html` (initial SPA load), OR add a one-line `POST /api/track` call from the Vue router's `afterEach` hook for per-route tracking. Recommend the latter — it matches the current per-page semantics.

### 13. Migration / cutover strategy

Two workable approaches:

**A. Big-bang on a branch (recommended for a solo/small team):**
Build the full FastAPI + Vue stack on a feature branch, test end-to-end with a Railway preview deployment, then swap in one PR. Fastest, lowest complexity, acceptable for a read-only dashboard with no user sessions.

**B. Strangler pattern:**
Run both apps side-by-side (FastHTML on port A, FastAPI on port B), gradually move routes. Overkill here — no persistent user state, no background jobs, no sessions to migrate.

**Recommend A.**

### 14. Risk register

| Risk | Mitigation |
|---|---|
| Mosaic Lite licensing edge case | Confirm MIT license on specific components before copying; fall back to raw Tailwind if unclear. |
| SPA breaks SEO for race result searches | Add `@vueuse/head` + pre-render key pages at build; acceptable for niche sport with direct-link traffic. |
| Broken existing URLs after launch | Add 6 redirect patterns at FastAPI root → `/app/...` equivalents. |
| Railway cold start with Vite static serving | Pre-warm via health check; static files are small (~200 KB gzipped target). |
| Visit tracking regression | Write one integration test that hits `/app/*` and asserts a Visit row is created. |
| Alembic migration skew in monorepo | Keep `alembic/` under `backend/`, run `alembic -c backend/alembic.ini upgrade head` in Docker CMD — same pattern as today. |

---

## Execution plan (ordered phases)

Each phase ends with a working app — no half-states shipped.

### Phase 0 — Repo reshape (no behavior change)
1. Create `backend/` dir, `git mv` existing `src/ alembic/ alembic.ini scripts/ tests/ main.py start.sh requirements.txt .env.example` under it.
2. Update `Dockerfile` and `docker-compose.yml` paths. Confirm the FastHTML app still boots unchanged.
3. **Checkpoint**: `docker compose up` produces the same app at `:5001`.

### Phase 1 — FastAPI backend in parallel
1. `backend/app/` skeleton: `main.py` (app factory), `config.py` (moved), `db/` (moved), `services/` (moved).
2. Replace `python-fasthtml` with `fastapi`, `uvicorn[standard]`, `pydantic>=2` in `pyproject.toml` (drop `requirements.txt`).
3. Port all 11 routes to `/api/*` JSON endpoints (routers in `app/api/`), Pydantic schemas in `app/schemas/`.
4. Keep `tests/test_standings_2025.py`; add `tests/test_api.py` (one test per endpoint).
5. Add `/health` and root `/` → 302 `/app/`.
6. Add backward-compat redirect routes (`/{year}`, `/{year}/{category}`, etc. → `/app/...`).
7. Visit-tracking middleware scoped to `/app/*` with HTML accept, or `POST /api/track` endpoint (recommend the latter).
8. **Checkpoint**: `pytest` green; hitting `/api/seasons/2026/standings/expert` returns the expected JSON.

### Phase 2 — Vue frontend scaffold
1. `frontend/` with Vite + Vue 3 + TypeScript + Pinia + Vue Router.
2. Tailwind 3, Mosaic Lite integration (cherry-pick components), `darkMode: 'class'`, custom palette (`primary` gold, `podium-*` metallics, elevated slate surfaces).
3. `App.vue` shell: Mosaic Lite sidebar + topbar, themed dark, with YearSwitcher and CategoryTabs.
4. `router/index.ts`: all 6 routes under `/app/*`, history mode, base `/app/`.
5. `api/client.ts` + generated TS types (consider `openapi-typescript` against FastAPI's `/openapi.json`).
6. **Checkpoint**: `npm run dev` shows the shell on `:5173` with hot reload against live backend.

### Phase 3 — Port each view
One PR per view (6 total) — easy to review, easy to revert:
1. StandingsView (most complex — stat cards, table, category tabs, event columns).
2. EventsView (calendar table).
3. EventResultsView (position + timing).
4. EventOverviewView (category selector landing).
5. RiderView (header + results table + disambiguation).
6. StatsView (visit analytics).

Each view reuses `PositionBadge`, `PointsPill`, `TimeCell`, `EmptyState`, `LoadingSkeleton`, `ErrorState`.

### Phase 4 — Single-container production build
1. Multi-stage `Dockerfile` (node build → python runtime).
2. FastAPI mounts `frontend/dist` at `/app/*` via `StaticFiles`, with SPA fallback catch-all that serves `dist/index.html` for any non-file path.
3. Update `railway.json` if needed (likely untouched — still `builder: DOCKERFILE`).
4. **Checkpoint**: Railway preview deploy works end-to-end from a feature branch.

### Phase 5 — Cutover + polish
1. Smoke test all 6 views against production data.
2. Verify visit tracking increments as expected.
3. Confirm redirects from legacy URLs work.
4. Merge to `main`; delete `python-fasthtml` and `src/ui/` imports / dead modules.
5. Update `DEPLOYMENT.md` and `CLAUDE.md`.

---

## Critical files

**To create:**
- `backend/app/main.py` — FastAPI app factory + static mount + SPA fallback
- `backend/app/api/{seasons,standings,events,results,riders,stats}.py`
- `backend/app/schemas/*.py` — Pydantic models
- `backend/app/middleware/visit_tracker.py`
- `backend/pyproject.toml`
- `frontend/` (whole tree)
- `Dockerfile` (multi-stage replacement)
- `.plan/refactor-plan.md` (this plan, post-approval)

**To modify:**
- `docker-compose.yml` (paths to backend/frontend)
- `railway.json` (only if health path changes)
- `start.sh` (new local dev story)
- `DEPLOYMENT.md`
- `CLAUDE.md` (add Vue/FastAPI conventions)

**To reuse as-is (moved into `backend/`):**
- `src/services/standings.py` → `backend/app/services/standings.py`
- `src/services/rider.py` → `backend/app/services/rider.py`
- `src/db/models.py` → `backend/app/db/models.py`
- `src/seasons.py` → `backend/app/seasons.py`
- `alembic/` → `backend/alembic/`
- `scripts/import_2025.py`, `scripts/import_event.py`, `scripts/seed_calendar.py`
- `tests/test_standings_2025.py`

**To delete (after cutover):**
- `src/ui/` (entire dir — layout, styles, standings, event_detail, rider_detail, events_page, common)
- `src/routes.py` (replaced by `app/api/*`)
- `main.py` (FastHTML entry; replaced by FastAPI `app.main:app`)
- `python-fasthtml` from deps

---

## Verification

**Backend:**
```bash
cd backend
pytest                                        # standings golden test + new API contract tests
uvicorn app.main:app --port 5001
curl -s localhost:5001/health                 # {"status":"ok",...}
curl -s localhost:5001/api/seasons/2026/standings/expert | jq '.rows | length'
curl -I localhost:5001/2026/expert            # 302 → /app/2026/expert
```

**Frontend:**
```bash
cd frontend
npm run build                                 # produces dist/; no TS errors
npm run test                                  # Vitest component tests
npm run dev                                   # :5173, visit /app/2026 and click through views
```

**Full stack in Docker:**
```bash
docker compose up --build
open http://localhost:5001/                   # 302 /app/ → StandingsView renders
open http://localhost:5001/2026/expert        # 302 /app/2026/expert → loads
curl http://localhost:5001/api/stats | jq     # JSON
```

**Railway:**
1. Push feature branch; Railway preview deploys the new Dockerfile.
2. Visit the preview URL — walk each of the 6 views.
3. Check logs for alembic migrations + visit tracking.
4. Merge to `main`.

---

## Open follow-ups (out of scope for this refactor)

- SSR / pre-render for SEO — decide before/after launch based on analytics.
- Internationalization (Bulgarian UI) — separate feature if desired.
- Move to server-timing headers + a real metrics endpoint.
- Switch from `requirements.txt`-style to `uv` throughout — already in the Dockerfile above; cement in pyproject.toml.
